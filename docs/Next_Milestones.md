# Next major pass

Implementation starts from 3bbf5e7 in an isolated local worktree. The running user application and its data are excluded from all checks.

Baseline: disposable production browser fixture `output/playwright/ux-before-QyiP8e`; representative branches, loops, multiple diagrams, long content, empty project, and current/stale/missing/ambiguous/imported Python evidence. Captured 1440×900, 1280×800, narrow, and actual 200% browser zoom; no external browser requests or runtime errors.

Visual direction: retain the calm diagram workspace with readable utility controls and temporary focused workspaces instead of more permanent panels.

Priority gaps: same-tab-only proposal recovery; slow text-heavy review; no persistent structured brief or distinct implementation tasks; no separately authorized execution path; installation still requires a runtime.

Implementation sequence, with a usable local commit after each gate:

1. Tidy preview, horizontal/vertical and whole/selected scope; persisted optional pins; deterministic cycle-aware layout and exact single-action undo. Preserve collision-aware labels.
2. Database proposal drafts, revision checks, operation receipts, durable Apply intent and explicit conflict/discard recovery.
3. Next/previous change navigation, compact counts and navigable advisory checks in the existing review workspace.
4. Structured project brief in manual history, portable exports and a new frozen planning instruction version.
5. Separate Build tasks with explicit criteria, node links and acyclic prerequisites; manual and reviewed agent editing.
6. One-step execution from a fresh approved snapshot in an explicitly selected disposable Git worktree; separate Run, code acceptance and checkout application, durable evidence and recovery.
7. Repeatable Windows portable packaging and CI artifacts; independent installed-package verification and current documentation.

Use existing pytest/Vitest/browser fixtures, plus focused meaningful regression cases for each gate. Final verification includes production/release builds, installed-package browser suites, keyboard/focus, reduced motion, zoom and full-diagram PNG bounds. No push, publication, real-user-repository execution or user-data mutation is authorized in this pass.
